% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression_analysis.R
\name{analyze_funding_logistic}
\alias{analyze_funding_logistic}
\title{Analyze NGO Funding Decision with Multiple Logistic Regression}
\usage{
analyze_funding_logistic(
  data,
  outcome = "funded",
  predictors,
  vif_threshold = 5,
  cooks_threshold = 1,
  remove_influential = TRUE,
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame containing NGO funding data}

\item{outcome}{Character string specifying binary outcome variable (default: "funded")}

\item{predictors}{Character vector of predictor variable names}

\item{vif_threshold}{Numeric threshold for VIF to detect multicollinearity (default: 5)}

\item{cooks_threshold}{Numeric threshold for Cook's distance (default: 4/n)}

\item{remove_influential}{Logical, whether to remove influential points (default: TRUE)}

\item{alpha}{Significance level for tests (default: 0.05)}
}
\value{
A list containing:
  \item{final_model}{The fitted glm object after removing problematic observations}
  \item{initial_model}{The initial glm object before any removals}
  \item{removed_predictors}{Character vector of predictors removed due to multicollinearity}
  \item{influential_points}{Indices of influential observations removed}
  \item{vif}{Final variance inflation factors}
  \item{performance}{Model performance metrics (accuracy, AUC, confusion matrix)}
  \item{summary}{Model summary statistics}
}
\description{
Performs comprehensive logistic regression analysis to identify predictors
of funding decision (funded vs not funded). Includes multicollinearity detection,
influential point removal, and model performance metrics.
}
